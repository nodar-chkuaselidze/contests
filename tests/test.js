'use strict';

var should = require('should');

describe('Test of tester', function () {
  it('Should just appear', function () {
    (2 + 3).should.be.exactly(5);
  });

  it('Should appear too', function () {
    (2 + 3).should.be.exactly(5);
  });
});
